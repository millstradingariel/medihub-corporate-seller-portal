# Use Node 20 to satisfy Firebase/Sanity requirements
FROM node:20-slim

WORKDIR /app

# Copy ONLY backend files
COPY backend/package*.json ./
RUN npm install

# Copy the rest of the backend code
COPY backend/ .

# Ensure port 8080 is exposed
EXPOSE 8080

CMD ["node", "server.js"]